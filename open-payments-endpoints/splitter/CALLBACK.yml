info:
  name: CALLBACK
  type: http
  seq: 4

http:
  method: GET
  url: "{{split_host}}/split/callback"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar('access_token', res.body.access_token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
