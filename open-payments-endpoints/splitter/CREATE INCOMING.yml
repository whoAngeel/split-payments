info:
  name: CREATE INCOMING
  type: http
  seq: 1

http:
  method: POST
  url: "{{split_host}}/incoming-payment"
  body:
    type: json
    data: |-
      {
        "wallet": "https://ilp.interledger-test.dev/angeel",
        "amount": "500"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: bru.setVar('incoming_payment_id', res.body.id);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
