info:
  name: "WALLET INFO "
  type: http
  seq: 3

http:
  method: GET
  url: "{{split_host}}/wallet"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
