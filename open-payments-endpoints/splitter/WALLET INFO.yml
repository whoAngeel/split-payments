info:
  name: "WALLET INFO "
  type: http
  seq: 3

http:
  method: GET
  url: "{{split_host}}/wallet/?url=https://ilp.interledger-test.dev/mochi"
  params:
    - name: url
      value: https://ilp.interledger-test.dev/mochi
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
