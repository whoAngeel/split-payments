info:
  name: LIST GALLERIES
  type: http
  seq: 4

http:
  method: GET
  url: "{{gallery_host}}/api/galleries"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
