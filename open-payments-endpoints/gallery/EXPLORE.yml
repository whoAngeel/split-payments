info:
  name: EXPLORE
  type: http
  seq: 13

http:
  method: GET
  url: "{{gallery_host}}/api/explore/products"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
