info:
  name: GET ARTISAN
  type: http
  seq: 8

http:
  method: GET
  url: "{{gallery_host}}/api/artisans/{{artisan_id}}"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
