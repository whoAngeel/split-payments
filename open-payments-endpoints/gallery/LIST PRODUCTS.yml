info:
  name: LIST PRODUCTS
  type: http
  seq: 10

http:
  method: GET
  url: "{{gallery_host}}/api/artisans/{{artisan_id}}/products"
  headers:
    - name: Authorization
      value: "Bearer {{token}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
