# Gin for HTTP routing

Both Gallery and Splitter APIs use [gin](https://github.com/gin-gonic/gin) as the HTTP framework.

## Why

- Most popular Go HTTP framework with mature middleware ecosystem
- Request binding, validation, and JSON rendering out of the box
- Team preference from prior experience
- Sufficiently lightweight — still uses `net/http` underneath, so custom middleware stays portable
